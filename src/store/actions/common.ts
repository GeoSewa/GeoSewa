import { commonSlice } from "../slices/common";

export const {
  setCommonState,
  toggleModal,
  setModalContent,
  togglePromptDialog,
  setToggleNavbar,
  setPromptDialogContent,
} = commonSlice.actions;
